{% extends 'base.html.twig' %}

{% block title %}Trick{% endblock %}

{% block body %}

    <div class="container">
        <section class="text-center">

            <h1>Figure: {{ trick.name }}</h1>
                <img src="https://picsum.photos/800/365" alt="img card"/>


            <h3> {{ trick.description }}</h3>

            <p>{{ trick.createdAt ? trick.createdAt|date('d-m-Y') : '' }}</p>

            <p>écrit par: {{ trick.author.username }}</p>

        </section>
            <a href="{{ path('trick_index') }}">back to list</a>

            <a href="{{ path('trick_edit', {'id': trick.id}) }}">edit</a>

            {{ include('trick/_delete_form.html.twig') }}
    </div>

{% endblock %}
